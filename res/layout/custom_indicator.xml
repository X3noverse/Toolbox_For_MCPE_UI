<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/isb_indicator_square_corners">
    <!--if you does not want to change this text by progress,
           you can set the TextView's id to others(not isb_progress).-->
    <TextView
        android:id="@id/isb_progress"
        android:layout_width="40.0dip"
        android:layout_height="26.6dip"
        android:gravity="center"
        android:maxLines="1"
        android:text="34"
        android:ellipsize="end"
        android:minWidth="40.0dip"
        android:textColor="#FFFFFF"
        android:textSize="13dp" />

</LinearLayout>